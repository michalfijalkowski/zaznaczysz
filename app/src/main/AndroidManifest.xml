<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.zaznaczysz">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_vote"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_vote"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".DiscussionActivity"></activity>
        <activity android:name=".EventListActivity" />
        <activity android:name=".PropositionListActivity" />
        <activity android:name=".UserPanelActivity" />
        <activity android:name=".PropositionActivity" />
        <activity android:name=".PropositionRankingActivity" />
        <activity android:name=".UserRankingActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".CreatePropositionActivity" />
        <activity android:name=".GroupActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>